---
description: Ouvrir le shell Django dans Docker
---

Ouvre un shell Django interactif dans le container Docker.

Exécute : `docker-compose exec web python manage.py shell`

Permet d'interagir avec les modèles et de tester du code Python.
